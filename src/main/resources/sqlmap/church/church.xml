<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="church.Church">

	<!-- ===============================================================
		
		 ============================================================ -->
	<!-- 지구코드별 성당 조회 -->
	<select id="selectChurchListInGigu" parameterType="hashmap" resultType="hashmap">
		<!-- <include refid="common.pagingPre"/> -->
			SELECT /* church.church.selectChurchListInGigu */ C.GIGU_CODE, D.NAME GIGU_NAME, C.CHURCH_IDX, C.NAME
			FROM CHURCH C, CODE_INSTANCE D
			WHERE C.GIGU_CODE = D.CODE_INST
			AND D.CODE='000004'
			ORDER BY C.GIGU_CODE ASC, C.NAME ASC
		<!-- <include refid="common.pagingPost"/> -->
	</select>
	
	
	
</mapper>